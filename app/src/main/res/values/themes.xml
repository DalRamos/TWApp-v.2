<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TWApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TWApp" parent="Base.Theme.TWApp" />

    <style name="Theme.App.TWApp" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/chip_pressed_color</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/twa</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.TWApp</item>
    </style>
</resources>